export class User {
    id: number = 0; // valor por defecto 0
    username: string = ''; // valor por defecto cadena vacía
    password: string = ''; // valor por defecto cadena vacía
    email: string = ''; // valor por defecto cadena vacía
    firstName: string = ''; // valor por defecto cadena vacía
    lastName: string = ''; // valor por defecto cadena vacía
    enabled: boolean = true; // valor por defecto true
  }